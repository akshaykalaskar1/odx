defaultOnlyFieldDetails.overrideLabel = DFContext.OverrideLabelValue;
      const pageHasNonEditableField = PCore.getStoreValue('pageHasNonEdtiableField', '', 'app');
      const context = PCore.getContainerUtils().getActiveContainerItemName(
        `${PCore.getConstants().APP.APP}/primary`
      );
      let editableFieldsCount = PCore.getFormUtils().getEditableFields(
        PCore.getContainerUtils().getActiveContainerItemContext(`${context}/workarea`)
      ).length;

      if (
        editableFieldsCount === 1 &&
        PCore.getFormUtils().getEditableFields(
          PCore.getContainerUtils().getActiveContainerItemContext(`${context}/workarea`)
        )[0].label === 'GoBack'
      ) {
        editableFieldsCount = 0;
      }

      if (editableFieldsCount === 1 && !pageHasNonEditableField) {
        defaultOnlyFieldDetails.isOnlyField = true;
      } 
